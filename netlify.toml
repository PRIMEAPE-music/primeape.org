# Netlify configuration for PRIMEAPE website

[build]
  publish = "dist"
  command = "npm run build"

# Build environment
[build.environment]
  NODE_VERSION = "18"

# Netlify Forms are automatically detected from the HTML
# No plugin installation needed - Netlify detects the hidden form in index.html